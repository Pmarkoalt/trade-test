# Development dependencies for trading-system
#
# NOTE: This file is auto-generated from pyproject.toml for convenience.
# The source of truth is pyproject.toml. To install dev dependencies:
#   pip install -e ".[dev]"
#   OR
#   pip install -r requirements-dev.txt
#
# For the recommended approach using pyproject.toml:
#   pip install -e ".[dev]"

# Testing
pytest>=7.0.0,<8.0.0
pytest-cov>=4.0.0,<6.0.0
pytest-benchmark>=4.0.0,<5.0.0
hypothesis>=6.0.0,<7.0.0

# Code quality
black>=23.0.0,<25.0.0
flake8>=6.0.0,<8.0.0
autoflake>=2.0.0,<3.0.0
mypy>=1.0.0,<2.0.0
types-PyYAML>=6.0.0,<7.0.0

# Coverage
coverage>=7.0.0,<8.0.0

# TOML parsing (for Python < 3.11)
tomli>=2.0.0,<3.0.0; python_version < '3.11'

# Documentation
sphinx>=7.0.0,<8.0.0
sphinx-rtd-theme>=1.3.0,<2.0.0
myst-parser>=2.0.0,<3.0.0  # Markdown support for Sphinx

# API dependencies (required for tests)
aiohttp>=3.8.0,<4.0.0  # Required for tests that use AlphaVantageNewsClient and BinanceClient

# ML dependencies (for ML refinement tests)
scikit-learn>=1.0.0,<2.0.0  # Required for ML refinement model tests

# Sentiment analysis (for news/sentiment tests)
vaderSentiment>=3.3.0,<4.0.0  # Required for sentiment analysis tests

# Dashboard dependencies (for dashboard tests)
streamlit>=1.20.0,<2.0.0  # Required for dashboard tests
plotly>=5.0.0,<6.0.0  # Required for dashboard visualization tests

# Data storage format (for data pipeline cache tests)
pyarrow>=10.0.0,<19.0.0  # Required for parquet file tests
